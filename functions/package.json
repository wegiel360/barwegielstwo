{
  "name": "bar-wegielstwo-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Bar Węgielstwo",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "@types/node": "^20.10.0"
  },
  "engines": {
    "node": "20"
  },
  "private": true
}